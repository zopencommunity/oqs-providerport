diff --git a/CMakeLists.txt b/CMakeLists.txt
index 782a0c0..e19be56 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -64,22 +64,58 @@ endif()
 endif()
 
 # Add required include for liboqs
-find_package(liboqs REQUIRED)
-get_target_property(LIBOQS_INCLUDE_DIR OQS::oqs INTERFACE_INCLUDE_DIRECTORIES)
-get_target_property(LIBOQS_LIBRARY_TYPE OQS::oqs TYPE)
-message(STATUS "liboqs found: Include dir at ${LIBOQS_INCLUDE_DIR} (${LIBOQS_LIBRARY_TYPE})")
-include_directories(${LIBOQS_INCLUDE_DIR})
+find_package(liboqs QUIET)
+if (TARGET OQS::oqs)
+    get_target_property(LIBOQS_INCLUDE_DIR OQS::oqs INTERFACE_INCLUDE_DIRECTORIES)
+    get_target_property(LIBOQS_LIBRARY_TYPE OQS::oqs TYPE)
+    message(STATUS "liboqs found via CMake target: ${LIBOQS_INCLUDE_DIR} (${LIBOQS_LIBRARY_TYPE})")
+else()
+    message(STATUS "liboqs CMake config not found, falling back to manual detection")
+     # ---- fallback: environment variable ----
+     if (DEFINED ENV{LIBOQS_HOME})
+	set(liboqs_INCLUDE_DIR "$ENV{LIBOQS_HOME}/include")
+	set(liboqs_LIBRARY "$ENV{LIBOQS_HOME}/lib/liboqs.a")
+	message(STATUS "Using LIBOQS_HOME: ${liboqs_INCLUDE_DIR} and ${liboqs_LIBRARY}")
+     # ---- fallback: manual search ----
+      else()
+	 message(STATUS "Looking for liboqs include directory")
+	 find_path(liboqs_INCLUDE_DIR NAMES oqs/oqs.h REQUIRED)
+	 message(STATUS "Found liboqs include directory: ${liboqs_INCLUDE_DIR}")
+
+	 message(STATUS "Looking for liboqs library")
+	 find_library(liboqs_LIBRARY NAMES oqs liboqs.a REQUIRED)
+	 message(STATUS "Found liboqs library: ${liboqs_LIBRARY}")
+      endif()
+      if (NOT liboqs_INCLUDE_DIR OR NOT liboqs_LIBRARY)
+	 message(FATAL_ERROR
+	     "liboqs not found. Install liboqs with CMake support or set LIBOQS_HOME.")
+      endif()
+
+       include_directories(${liboqs_INCLUDE_DIR})
+       add_library(OQS::oqs STATIC IMPORTED)
+       set_target_properties(OQS::oqs PROPERTIES
+             IMPORTED_LOCATION "${liboqs_LIBRARY}"
+             INTERFACE_INCLUDE_DIRECTORIES "${liboqs_INCLUDE_DIR}"
+       )
+endif()
+
+# ------------------------------------------------------------
+# Static provider helper
+# ------------------------------------------------------------
 
 # Hints the compiler on the fact that the provider is being compiled into a static library.
 function(targets_set_static_provider)
-  foreach(target ${ARGN})
-    target_compile_definitions(${target} PRIVATE "OQS_PROVIDER_STATIC")
-    if(NOT target STREQUAL oqsprovider)
-      target_link_libraries(${target} PRIVATE oqsprovider)
-    endif()
-  endforeach()
+	foreach(target IN LISTS ARGN)
+	      target_compile_definitions(${target} PRIVATE OQS_PROVIDER_STATIC)
+
+              # Link against oqsprovider except for oqsprovider itself
+	      if (TARGET oqsprovider AND NOT target STREQUAL oqsprovider)
+	            target_link_libraries(${target} PRIVATE oqsprovider)
+              endif()
+        endforeach()
 endfunction()
 
+
 # Provider module
 add_subdirectory(oqsprov)
 
